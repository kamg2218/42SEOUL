가변적인 함수 <stdarg.h>

- "..." 사용 : 추가로 인자를 전달할 수 있다는 것을 표시한다.

가변적인 인수를 받아들이는 데에 stdarg.h 헤더를 사용한다.
 -> va_list, va_start, va_arg, va_end

va_list : 포인터의 데이터형으로 받아들이는 추가 인자들을 저장하는 공간이다.
va_start() : va_list 데이터형 포인터를 초기화한다.
va_arg() : 추가 인자들을 차례대로 읽어들인다.
va_end() : 마지막에 정리하는 동작을 한다.